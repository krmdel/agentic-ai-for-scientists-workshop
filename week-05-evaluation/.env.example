# Week 5 — environment template. Copy to .env (local) or add as Colab Secrets.
# Only NB01 (LLM-as-judge) needs a key. NB00/02/03 need only a GPU.

# REQUIRED for NB01 — free, no card: https://aistudio.google.com/apikey
# The Gemini model that acts as the judge (pointwise + pairwise scoring).
GOOGLE_API_KEY=AIza...
